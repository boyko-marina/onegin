#ifndef COMPARE_STRINGS
#define COMPARE_STRINGS

int str_len(const char *s);
int compare_strings_from_start(const char *par1, const char *par2);
int compare_strings_from_end(const char *par1, const char *par2);

#endif
